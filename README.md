# culling-gamer

### Before you run
- Create a config.json file and add
```
{
	"token": "BOT_TOKEN",
	"clientId": "CLIENT_ID",
	"guildId": "GUILD_ID"
}
```
- Create a .env file and add
```
environment = "development"
```